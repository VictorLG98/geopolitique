# Geopolitique Environment Configuration

# If empty, the backend will fallback to a local SQLite database (geopolitique.db)
# To use your external Neon database, uncomment the line below and paste your connection string:
# DATABASE_URL=postgresql://user:password@hostname/dbname?sslmode=require

PORT=8000
HOST=0.0.0.0

# Admin secret key for dashboard authorization
ADMIN_SECRET=admin_geopolitique_2026